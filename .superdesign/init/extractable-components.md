# Extractable components

None.

There is no `components/` directory and no shared layout chrome (nav, sidebar, header, footer) beyond the root HTML shell in `app/layout.tsx`. Skip Superdesign component extraction until product primitives exist.
